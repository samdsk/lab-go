dati=read.csv("input")
View(dati)
summary(dati)
moda(dati)
library(dprep)
library("dprep")
Mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}
Mode(dati)
library("modeest")
estimate_mode <- function(x) {
d <- density(x)
d$x[which.max(d$y)]
}
estimate_mode(dati)
dati=read.csv("input")
View(dati)
summary(dati)
estimate_mode(dati)
mean(dati)
dati=read.csv("input")
View(dati)
mean(dati)
dati=read.csv("input")
mean(dati)
dati=read.csv("input")
